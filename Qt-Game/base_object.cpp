#include "base_object.h"
#include <QTimer>
#include <QGraphicsScene>
#include <qlist.h>
#include "enemy.h"
#include "powerup.h"
#include "static_game.h"

/*Base_Object::Base_Object(QGraphicsItem *parent):QObject() {

    QTimer * timer = new QTimer();
    connect(timer,SIGNAL(timeout()),this,SLOT(Base_Object->move()));

    timer->start(50);

};*/

